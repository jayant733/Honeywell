# Baseline deterministic policy settings

baseline_setpoints:
  occupied:
    heating: 21.0
    cooling: 24.0
  unoccupied:
    heating: 16.0
    cooling: 28.0

# Minimum change threshold to avoid micro-actuations (Celsius)
deadband: 0.5
